version: '3.8'

services:
  app:
    build: .
    container_name: trading-bot
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./trading.db:/app/trading.db # Persist SQLite DB
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Seoul
